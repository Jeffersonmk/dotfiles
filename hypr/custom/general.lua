hl.config({
    input = {
        -- 1. REMOVER ACELERAÇÃO DO MOUSE
        force_no_accel = true,
        accel_profile = "flat",
        sensitivity = 0.0, -- Deixe em 0.0 para input puro (ajuste se precisar)
    }
})
